<<<<<<< HEAD
{% extends 'base.html.twig' %}

{% block title %}Détails de l'utilisateur{% endblock %}

{% block body %}
    <h1>Détails de l'utilisateur</h1>

    <table>
        <tr>
            <th>Nom</th>
            <td>{{ user.nom }}</td>
        </tr>
        <tr>
            <th>Prénom</th>
            <td>{{ user.prenom }}</td>
        </tr>
        <tr>
            <th>Email</th>
            <td>{{ user.email }}</td>
        </tr>
        <tr>
            <th>Rôle</th>
            <td>{{ user.role }}</td>
        </tr>
        <tr>
            <th>Spécialité</th>
            <td>{{ user.specialite ? user.specialite : 'Non spécifiée' }}</td>
        </tr>
        <tr>
            <th>Image</th>
            <td><img src="{{ user.imageUrl }}" alt="Image de l'utilisateur" width="100"></td>
        </tr>
        <tr>
            <th>Date de naissance</th>
            <td>{{ user.dateNaissance|date('d-m-Y') }}</td>
        </tr>
    </table>

    <a href="{{ path('user_edit', {'id': user.id}) }}">Modifier</a>

    <form action="{{ path('user_delete', {'id': user.id}) }}" method="post" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet utilisateur ?')">
        <button type="submit">Supprimer</button>
    </form>

    <a href="{{ path('user_index') }}">Retour à la liste des utilisateurs</a>
{% endblock %}
=======
{% extends 'admin/homepageadmin.html.twig' %}

{% block title %}Détails Utilisateur - Admin{% endblock %}

{% block body %}
<div class="container-fluid px-4">
    <div class="d-sm-flex align-items-center justify-content-between mb-4">
        <h1 class="h3 mb-0 text-gray-800">
            <i class="fas fa-user-circle mr-2"></i>Détails Utilisateur
        </h1>
        <a href="{{ path('user_index') }}" class="btn btn-sm btn-primary shadow-sm">
            <i class="fas fa-arrow-left fa-sm mr-1"></i> Retour à la liste
        </a>
    </div>

    <div class="card shadow mb-4">
        <div class="card-header py-3 d-flex justify-content-between align-items-center bg-primary text-white">
            <h6 class="m-0 font-weight-bold">Fiche Utilisateur</h6>
            <div class="dropdown no-arrow">
                <a class="dropdown-toggle" href="#" role="button" id="dropdownMenuLink" 
                   data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    <i class="fas fa-ellipsis-v fa-sm fa-fw"></i>
                </a>
                <div class="dropdown-menu dropdown-menu-right shadow animated--fade-in" 
                     aria-labelledby="dropdownMenuLink">
                    <a class="dropdown-item" href="{{ path('app_user_update', {'id': user.id}) }}">
                        <i class="fas fa-edit fa-sm mr-2"></i> Modifier
                    </a>
                    <form action="{{ path('user_delete', {'id': user.id}) }}" method="post" 
                          onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet utilisateur ?');">
                        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
                        <button type="submit" class="dropdown-item text-danger">
                            <i class="fas fa-trash fa-sm mr-2"></i> Supprimer
                        </button>
                    </form>
                </div>
            </div>
        </div>
        
        <div class="card-body">
            <div class="row">
                <div class="col-md-4 text-center">
                    {% if user.imageUrl %}
                    <img src="{{ asset('Uploads/users/' ~ user.imageUrl) }}" 
                         class="img-profile rounded-circle mb-3" style="width: 200px; height: 200px; object-fit: cover;">
                    {% else %}
                    <div class="bg-secondary rounded-circle d-flex align-items-center justify-content-center mb-3" 
                         style="width: 200px; height: 200px;">
                        <span class="display-4 text-white">{{ user.prenom|first|upper }}{{ user.nom|first|upper }}</span>
                    </div>
                    {% endif %}
                    
                    <h4 class="font-weight-bold">{{ user.prenom }} {{ user.nom }}</h4>
                    <span class="badge 
                        {% if user.role == 'admin' %}badge-danger
                        {% elseif user.role == 'entraineur' %}badge-warning
                        {% elseif user.role == 'sportif' %}badge-primary
                        {% else %}badge-secondary{% endif %}">
                        {{ user.role|capitalize }}
                    </span>
                </div>
                
                <div class="col-md-8">
                    <div class="table-responsive">
                        <table class="table table-bordered" width="100%" cellspacing="0">
                            <tbody>
                                <tr>
                                    <th width="30%">ID</th>
                                    <td>{{ user.id }}</td>
                                </tr>
                                <tr>
                                    <th>Nom complet</th>
                                    <td>{{ user.prenom }} {{ user.nom }}</td>
                                </tr>
                                <tr>
                                    <th>Email</th>
                                    <td>{{ user.email }}</td>
                                </tr>
                                <tr>
                                    <th>Rôle</th>
                                    <td>
                                        <span class="badge 
                                            {% if user.role == 'admin' %}badge-danger
                                            {% elseif user.role == 'entraineur' %}badge-warning
                                            {% elseif user.role == 'sportif' %}badge-primary
                                            {% else %}badge-secondary{% endif %}">
                                            {{ user.role|capitalize }}
                                        </span>
                                    </td>
                                </tr>
                                <tr>
                                    <th>Date de naissance</th>
                                    <td>{{ user.dateNaissance ? user.dateNaissance|date('d/m/Y') : 'Non spécifiée' }}</td>
                                </tr>
                                {% if user.specialite %}
                                <tr>
                                    <th>Spécialité</th>
                                    <td>{{ user.specialite }}</td>
                                </tr>
                                {% endif %}
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
        
        <div class="card-footer bg-white">
            <div class="d-flex justify-content-end">
                {% set is_own_profile = app.user and app.user.id == user.id %}
                {% if is_granted('ROLE_ADMIN') and not is_own_profile %}
                    <a href="{{ path('app_user_update', {'id': user.id}) }}" class="btn btn-primary mr-2">
                        <i class="fas fa-edit mr-1"></i> Modifier
                    </a>
                    <form action="{{ path('user_delete', {'id': user.id}) }}" method="post" 
                          onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet utilisateur ?');">
                        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
                        <button type="submit" class="btn btn-danger">
                            <i class="fas fa-trash mr-1"></i> Supprimer
                        </button>
                    </form>
                {% endif %}
            </div>
        </div>
    </div>
</div>
{% endblock %}
>>>>>>> 1e2a521f379c042fb627b82253dcd3e5a8f8a1fc
