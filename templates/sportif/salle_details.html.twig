{% extends 'sportif/base.html.twig' %}

{% block title %}{{ salle.nom }} | Gym{% endblock %}

{% block body %}
    <!-- Salle Details Section -->
    <section class="salle-details-section">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-lg-6">
                    <div class="salle-image">
                        {% if salle.urlPhoto %}
                            <img src="{{ asset('Uploads/images/' ~ salle.urlPhoto) }}" alt="{{ salle.nom }}">
                            <div class="image-overlay"></div>
                        {% else %}
                            <img src="{{ asset('img/placeholder-gym.jpg') }}" alt="Default Gym Image">
                            <div class="image-overlay"></div>
                        {% endif %}
                    </div>
                </div>
                <div class="col-lg-6">
                    <div class="salle-content">
                        <h2>{{ salle.nom }}</h2>
                        <p>{{ salle.details }}</p>
                        <ul class="salle-meta">
                            <li><i class="fa fa-map-marker"></i> {{ salle.adresse }}</li>
                            <li><i class="fa fa-user"></i> {% if salle.responsable %}{{ salle.responsable.prenom|default('') }} {{ salle.responsable.nom|default('N/A') }}{% else %}No responsible{% endif %}</li>
                            <li><i class="fa fa-phone"></i> {{ salle.numTel }}</li>
                            <li><i class="fa fa-envelope"></i> {{ salle.email }}</li>
                        </ul>
                        <div class="salle-actions">
                            <a href="#abonnements" class="primary-btn">Voir les abonnements</a>
                            <a href="#evenements" class="primary-btn">Voir les événements</a>
                            <a href="{{ path('home') }}" class="secondary-btn">Retour</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Section Abonnements -->
    <section id="abonnements" class="abonnements-section">
        <div class="container">
            <div class="section-title">
                <h2>Nos Abonnements</h2>
                <p>Découvrez nos offres dynamiques pour booster votre forme</p>
            </div>
            <div class="row">
                {% for abonnement in abonnements %}
                <div class="col-lg-4 col-md-6">
                    <div class="abonnement-card">
                        <div class="abonnement-header">
                            <h3>{{ abonnement.type.value|capitalize }}</h3>
                            <div class="price">{{ abonnement.tarif }} DT</div>
                        </div>
                        <div class="abonnement-body">
                            <p><i class="fa fa-dumbbell"></i> {{ abonnement.activite.nom }}</p>
                            <p><i class="fa fa-calendar"></i> Durée: 
                                {% if abonnement.type.value == 'mois' %}30 jours
                                {% elseif abonnement.type.value == 'trimestre' %}90 jours
                                {% elseif abonnement.type.value == 'année' %}365 jours
                                {% else %}Unknown{% endif %}
                            </p>
                            <a href="#" class="subscribe-btn" title="Souscription bientôt disponible">S'abonner</a>
                        </div>
                    </div>
                </div>
                {% else %}
                <div class="col-12">
                    <div class="alert alert-info text-center">
                        <i class="fa fa-info-circle fa-2x mb-3"></i>
                        <p>Aucun abonnement disponible pour cette salle actuellement.</p>
                    </div>
                </div>
                {% endfor %}
            </div>
        </div>
    </section>

    <!-- Section Événements -->
    <section id="evenements" class="evenements-section">
        <div class="container">
            <div class="section-title">
                <h2>Nos Événements</h2>
                <p>Participez à nos événements pour une expérience unique</p>
            </div>
            <div class="row">
                {% for equipe_event in equipe_events %}
                <div class="col-lg-4 col-md-6">
                    <div class="evenement-card">
                        <div class="evenement-image">
                            {% if equipe_event.event.imageUrl %}
                                <img src="{{ asset('Uploads/evenements/' ~ equipe_event.event.imageUrl) }}" alt="{{ equipe_event.event.nom }}">
                            {% else %}
                                <img src="{{ asset('img/event-default.jpg') }}" alt="Default Event Image">
                            {% endif %}
                            <div class="evenement-date">
                                <span>{{ equipe_event.event.date|date('d') }}</span>
                                <p>{{ equipe_event.event.date|date('M') }}</p>
                            </div>
                        </div>
                        <div class="evenement-body">
                            <h3>{{ equipe_event.event.nom }}</h3>
                            <p><i class="fa fa-clock-o"></i> {{ equipe_event.event.heureDebut|date('H:i') }} - {{ equipe_event.event.heureFin|date('H:i') }}</p>
                            <p>{{ equipe_event.event.description|slice(0, 100) }}...</p>
                            <a href="{{ path('app_equipe_event_show', {'id': equipe_event.id}) }}" class="event-btn">Détails</a>
                        </div>
                    </div>
                </div>
                {% else %}
                <div class="col-12">
                    <div class="alert alert-info text-center">
                        <i class="fa fa-info-circle fa-2x mb-3"></i>
                        <p>Aucun événement prévu pour cette salle actuellement.</p>
                    </div>
                </div>
                {% endfor %}
            </div>
        </div>
    </section>

   <style>

    /* Global Styles */
    @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap');
    * {
        font-family: 'Poppins', sans-serif;
        box-sizing: border-box;
    }

    /* Couleurs thématiques */
    :root {
        --primary: #FF6B35;  /* Orange vif */
        --secondary: #004E89; /* Bleu sportif */
        --dark: #1A1A1A;
        --light: #F7F7F7;
        --accent: #FFD166;   /* Jaune accent */
    }

    /* Salle Details Section - Version Dynamique */
    .salle-details-section {
        padding: 100px 0;
        background: linear-gradient(135deg, var(--dark), #2A2A2A);
        color: white;
        position: relative;
        overflow: hidden;
    }

    .salle-image {
        position: relative;
        border-radius: 15px;
        overflow: hidden;
        box-shadow: 0 15px 40px rgba(0, 0, 0, 0.3);
        transform: perspective(1000px) rotateY(-5deg);
        transition: all 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275);
    }

    .salle-image:hover {
        transform: perspective(1000px) rotateY(0deg) scale(1.02);
        box-shadow: 0 20px 50px rgba(0, 0, 0, 0.4);
    }

    .salle-image img {
        width: 100%;
        height: auto;
        transition: transform 0.8s ease;
    }

    .image-overlay {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: linear-gradient(to bottom, rgba(0, 0, 0, 0.1), rgba(0, 78, 137, 0.3));
        z-index: 1;
    }

    .salle-content h2 {
        font-size: 3rem;
        font-weight: 800;
        color: white;
        margin-bottom: 1.5rem;
        text-transform: uppercase;
        letter-spacing: 1.5px;
        position: relative;
        display: inline-block;
    }

    .salle-content h2::after {
        content: '';
        position: absolute;
        bottom: -10px;
        left: 0;
        width: 70px;
        height: 4px;
        background: linear-gradient(90deg, var(--primary), var(--accent));
        border-radius: 2px;
    }

    .salle-content p {
        font-size: 1.1rem;
        line-height: 1.8;
        color: rgba(255, 255, 255, 0.85);
        margin-bottom: 2rem;
    }

    .salle-meta {
        padding-left: 0;
        list-style: none;
    }

    .salle-meta li {
        font-size: 1rem;
        margin-bottom: 1rem;
        display: flex;
        align-items: center;
        gap: 12px;
        color: rgba(255, 255, 255, 0.8);
    }

    .salle-meta i {
        color: var(--primary);
        width: 24px;
        text-align: center;
        font-size: 1.2rem;
    }

    /* Boutons - Version Dynamique */
    .salle-actions {
        margin-top: 2.5rem;
        display: flex;
        flex-wrap: wrap;
        gap: 1rem;
    }

    .primary-btn, .secondary-btn {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        padding: 0.9rem 2rem;
        border-radius: 50px;
        font-size: 0.95rem;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 1px;
        transition: all 0.4s cubic-bezier(0.25, 0.8, 0.25, 1);
        min-width: 180px;
        text-decoration: none;
        border: none;
        cursor: pointer;
        position: relative;
        overflow: hidden;
        z-index: 1;
    }

    .primary-btn {
        background: linear-gradient(45deg, var(--primary), #FF8C42);
        color: white;
        box-shadow: 0 4px 20px rgba(255, 107, 53, 0.3);
    }

    .primary-btn::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: linear-gradient(45deg, #FF8C42, var(--primary));
        opacity: 0;
        transition: opacity 0.4s ease;
        z-index: -1;
    }

    .primary-btn:hover {
        transform: translateY(-3px);
        box-shadow: 0 8px 25px rgba(255, 107, 53, 0.4);
    }

    .primary-btn:hover::before {
        opacity: 1;
    }

    .secondary-btn {
        background: transparent;
        color: white;
        border: 2px solid var(--primary);
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
    }

    .secondary-btn:hover {
        background: rgba(255, 107, 53, 0.1);
        transform: translateY(-3px);
        box-shadow: 0 8px 20px rgba(0, 0, 0, 0.15);
    }

    /* Section Abonnements - Design Moderne */
    .abonnements-section {
        padding: 5rem 0;
        background: var(--light);
    }

    .section-title {
        text-align: center;
        margin-bottom: 3rem;
    }

    .section-title h2 {
        font-size: 2.5rem;
        font-weight: 800;
        color: var(--dark);
        text-transform: uppercase;
        position: relative;
        display: inline-block;
    }

    .section-title h2::after {
        content: '';
        position: absolute;
        bottom: -10px;
        left: 50%;
        transform: translateX(-50%);
        width: 80px;
        height: 4px;
        background: linear-gradient(90deg, var(--primary), var(--secondary));
        border-radius: 2px;
    }

    .section-title p {
        font-size: 1.1rem;
        color: #666;
        margin-top: 1rem;
    }

    .abonnement-card {
        background: white;
        border-radius: 15px;
        overflow: hidden;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.08);
        transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        margin-bottom: 2rem;
        border: none;
        position: relative;
        z-index: 1;
    }

    .abonnement-card:hover {
        transform: translateY(-10px);
        box-shadow: 0 15px 40px rgba(0, 0, 0, 0.15);
    }

    .abonnement-header {
        background: linear-gradient(135deg, var(--secondary), #0066B3);
        color: white;
        padding: 2rem;
        text-align: center;
        position: relative;
        overflow: hidden;
    }

    .abonnement-header::before {
        content: '';
        position: absolute;
        top: -50%;
        right: -50%;
        width: 100%;
        height: 200%;
        background: linear-gradient(135deg, rgba(255, 255, 255, 0.1), transparent);
        transform: rotate(30deg);
        z-index: -1;
    }

    .abonnement-header h3 {
        font-size: 1.5rem;
        font-weight: 700;
        margin: 0;
        text-transform: uppercase;
        letter-spacing: 1px;
    }

    .price {
        font-size: 2.5rem;
        font-weight: 800;
        margin-top: 0.5rem;
        display: block;
    }

    .abonnement-body {
        padding: 2rem;
        text-align: center;
    }

    .abonnement-body p {
        font-size: 1rem;
        color: #555;
        margin-bottom: 1.5rem;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 10px;
    }

    .abonnement-body i {
        color: var(--secondary);
        font-size: 1.2rem;
    }

    .subscribe-btn {
        display: inline-block;
        padding: 0.8rem 2rem;
        background: linear-gradient(45deg, var(--primary), #FF8C42);
        color: white;
        border-radius: 
</style>
{% endblock %}