<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Facture</title>
    <style>
        body { font-family: DejaVu Sans, Arial, Helvetica, sans-serif; margin: 40px; }
        h1, h2, h3, h4, h5, h6 { margin: 0; }
        .company {
            text-align: center;
            margin-bottom: 30px;
        }
        .company h2 { font-size: 2em; }
        .company p { margin: 2px 0; }
        .invoice-title {
            text-align: center;
            font-size: 2em;
            margin: 30px 0 20px 0;
            font-weight: bold;
        }
        table {
            width: 70%;
            margin: 0 auto 30px auto;
            border-collapse: collapse;
        }
        th, td {
            border: 1px solid #222;
            padding: 8px 12px;
            font-size: 1.1em;
        }
        th {
            background: #f5f5f5;
            text-align: left;
        }
        .footer {
            text-align: center;
            margin-top: 60px;
            color: #888;
            font-size: 1em;
        }
        .footer strong { color: #444; }
        .footer small { display: block; margin-top: 8px; color: #aaa; }
    </style>
</head>
<body>
    <div class="company">
        <h2>Votre Entreprise</h2>
        <p>123 Rue Example, Ville</p>
        <p>Tél: +216 XX XXX XXX</p>
        <p>Email: contact@example.com</p>
    </div>
    <div class="invoice-title">FACTURE</div>
    <table>
        <tr>
            <th>Numéro de Commande:</th>
            <td>{{ commande.idC }}</td>
        </tr>
        <tr>
            <th>Date:</th>
            <td>{{ commande.dateC|date('d/m/Y H:i:s') }}</td>
        </tr>
        <tr>
            <th>Statut:</th>
            <td>{{ commande.statutC|upper }}</td>
        </tr>
        <tr>
            <th>Total:</th>
            <td>{{ commande.totalC|number_format(2, '.', ' ') }} DT</td>
        </tr>
    </table>
    <div class="footer">
        <strong>Merci de votre confiance!</strong><br>
        <small>Conditions: Cette facture est valable pendant 30 jours.<br>
        Pour toute question, veuillez nous contacter.</small>
    </div>
</body>
</html> 